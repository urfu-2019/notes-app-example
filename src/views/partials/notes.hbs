{{#if notes.length}}
    <h2>Заметки</h2>
    <ul>
        {{#each notes}}
            <li><a href="/notes/{{ name }}">{{ name }}</a></li>
        {{/each}}
    </ul>
{{^}}
    <p>Notes not found!</p>
{{/if}}
